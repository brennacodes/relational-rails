<p id="notice"><%= notice %></p>

<h1>Investigations</h1>

<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th>Uid</th>
      <th>Active</th>
      <th>Active leads</th>
      <th>Department</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @investigations.each do |investigation| %>
      <tr>
        <td><%= link_to investigation.subject, investigation %></td>
        <td><%= investigation.uid %></td>
        <td><%= investigation.active %></td>
        <td><%= investigation.active_leads %></td>
        <td><%= investigation.department.name %></td>
        <td><%= link_to 'Delete', investigation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Investigation', new_investigation_path %>
